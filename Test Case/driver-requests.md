## **TC02 - driver-requests**
| **Test Case ID** | **Test Scenario**      | **Test Case**                                                  | **Preconditions**                                                                                | **Test Steps**                                                                                         | **Test Data**                                                      | **Expected Result**                                                                                                 | **Priority** |
| ---------------- | ---------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ------------ |
| TC-DRQ-001       | Gửi yêu cầu tài xế     | Gửi yêu cầu khi Ride ở `SEARCHING_DRIVER` và có tài xế phù hợp | Customer đã tạo Ride; Ride = `SEARCHING_DRIVER`; có Driver `AVAILABLE`, đúng loại xe, trong 5 km | 1. Đăng nhập bằng token hợp lệ.<br>2. Gửi `POST /rides/3001/driver-requests`.<br>3. Kiểm tra response. | `ride_id=3001`; Driver 201: `AVAILABLE`, `CAR`, khoảng cách 2.5 km | HTTP `202 Accepted`.<br>Yêu cầu được gửi đến tài xế phù hợp.<br>`search_radius_km=5`, `request_timeout_seconds=30`. | High         |
| TC-DRQ-002       | Gửi yêu cầu tài xế     | Chỉ chọn tài xế trong bán kính 5 km                            | Ride đang `SEARCHING_DRIVER`; có Driver trong và ngoài 5 km                                      | 1. Gửi yêu cầu tìm tài xế.<br>2. Kiểm tra danh sách `requests_sent`.                                   | D1: 4.9 km; D2: 5.0 km; D3: 5.1 km                                 | Chỉ Driver trong phạm vi 5 km được gửi request.<br>Driver ngoài phạm vi không nhận request.                         | High         |
| TC-DRQ-003       | Gửi yêu cầu tài xế     | Không gửi cho Driver không ở trạng thái `AVAILABLE`            | Có Driver `BUSY/OFFLINE` trong 5 km                                                              | 1. Tạo request tìm tài xế.<br>2. Kiểm tra `requests_sent`.                                             | D1 `AVAILABLE`; D2 `BUSY`; D3 `OFFLINE`                            | Chỉ Driver `AVAILABLE` được chọn.                                                                                   | High         |
| TC-DRQ-004       | Gửi yêu cầu tài xế     | Không gửi cho Driver sai loại xe                               | Ride yêu cầu `CAR`; Driver gần nhưng có `BIKE`                                                   | 1. Gửi yêu cầu tìm tài xế.<br>2. Kiểm tra Driver nhận request.                                         | Ride `vehicle_type=CAR`; Driver 201 `vehicle_type=BIKE`            | Driver sai loại xe không được gửi request.                                                                          | High         |
| TC-DRQ-005       | Gửi yêu cầu tài xế     | Ride không còn ở trạng thái `SEARCHING_DRIVER`                 | Ride = `DRIVER_ASSIGNED`                                                                         | 1. Gửi `POST /rides/3001/driver-requests`.<br>2. Kiểm tra response.                                    | `ride_id=3001`, status=`DRIVER_ASSIGNED`                           | HTTP `409 Conflict`.<br>Response Error có `code`, `message`.<br>Không gửi request mới.                              | High         |
| TC-DRQ-006       | Gửi yêu cầu tài xế     | Request của tài xế được tạo với timeout 30 giây                | Ride đang tìm tài xế                                                                             | 1. Gửi request.<br>2. Kiểm tra `request_timeout_seconds` và `expires_at`.                              | Sent time `10:00:00`; timeout `30`                                 | `request_timeout_seconds=30`.<br>`expires_at` tương ứng sau 30 giây.                                                | High         |
| TC-DRQ-007       | Tài xế phản hồi        | Tài xế chấp nhận request hợp lệ                                | Request 7001 thuộc Driver hiện tại; chưa hết hạn; Ride chưa có Driver                            | 1. Driver đăng nhập.<br>2. Gửi `POST /driver-requests/7001/respond`.<br>3. Kiểm tra response.          | `{ "decision": "ACCEPTED" }`                                       | HTTP `200 OK`.<br>`decision=ACCEPTED`.<br>Ride chuyển `DRIVER_ASSIGNED`.<br>Driver chuyển `BUSY`.                   | High         |
| TC-DRQ-008       | Tài xế phản hồi        | Tài xế từ chối request                                         | Request còn hạn và thuộc Driver                                                                  | 1. Gửi respond.<br>2. Chọn `DECLINED`.<br>3. Kiểm tra response.                                        | `{ "decision": "DECLINED" }`                                       | HTTP `200 OK`.<br>`decision=DECLINED`.<br>Ride tiếp tục `SEARCHING_DRIVER` nếu còn Driver khác.                     | High         |
| TC-DRQ-009       | Tài xế phản hồi        | Tài xế phản hồi sau 30 giây                                    | Request đã `EXPIRED`                                                                             | 1. Chờ request hết hạn.<br>2. Gửi `ACCEPTED`.<br>3. Kiểm tra response.                                 | `request_id=7001`; elapsed > 30s                                   | HTTP `409 Conflict`.<br>Error thể hiện request đã hết hạn.                                                          | High         |
| TC-DRQ-010       | Tài xế phản hồi        | Driver khác cố phản hồi request không thuộc mình               | Request 7001 thuộc Driver 201; đăng nhập Driver 202                                              | 1. Đăng nhập Driver 202.<br>2. Gửi respond cho request 7001.                                           | `{ "decision": "ACCEPTED" }`                                       | HTTP `403 Forbidden`.<br>Không thay đổi request/Ride.                                                               | High         |
| TC-DRQ-011       | Tranh chấp nhận chuyến | Hai Driver đồng thời nhận cùng Ride                            | Ride đang `SEARCHING_DRIVER`; có request gửi cho D201/D202                                       | 1. Gửi `ACCEPTED` đồng thời cho 2 request.<br>2. Kiểm tra kết quả.                                     | Request 7001 → D201; Request 7002 → D202                           | Chỉ request hợp lệ đầu tiên được `ACCEPTED`.<br>Ride chỉ có một Driver.<br>Request còn lại nhận `409`.              | High         |
| TC-DRQ-012       | Tài xế phản hồi        | Request đã có Driver khác nhận trước đó                        | Ride = `DRIVER_ASSIGNED`                                                                         | 1. Gửi `ACCEPTED` cho request còn lại.<br>2. Kiểm tra response.                                        | `request_id=7002`                                                  | HTTP `409 Conflict`.<br>Không thay đổi Driver đã được gán.                                                          | High         |