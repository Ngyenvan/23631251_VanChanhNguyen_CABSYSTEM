## **TC02 - driver-requests**

| **Test Case ID** | **Test Scenario** | **Test Case** | **Preconditions** | **Test Steps** | **Test Data** | **Expected Result** | **Priority** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-DRQ-001 | Send driver requests | Send a request when the Ride is `SEARCHING_DRIVER` and a suitable driver exists | Customer has created a Ride; Ride = `SEARCHING_DRIVER`; an `AVAILABLE` Driver with the correct vehicle type is within 5 km | 1. Log in with a valid token.<br>2. Send `POST /rides/3001/driver-requests`.<br>3. Check the response. | `ride_id=3001`; Driver 201: `AVAILABLE`, `CAR`, distance 2.5 km | HTTP `202 Accepted`.<br>Request is sent to suitable drivers.<br>`search_radius_km=5`, `request_timeout_seconds=30`. | High |
| TC-DRQ-002 | Send driver requests | Select only drivers within a 5 km radius | Ride is `SEARCHING_DRIVER`; drivers exist both inside and outside 5 km | 1. Send a driver search request.<br>2. Check `requests_sent`. | D1: 4.9 km; D2: 5.0 km; D3: 5.1 km | Requests are sent only to drivers within 5 km.<br>Drivers outside the radius do not receive requests. | High |
| TC-DRQ-003 | Send driver requests | Do not send to a Driver who is not `AVAILABLE` | `BUSY/OFFLINE` drivers are within 5 km | 1. Create a driver search request.<br>2. Check `requests_sent`. | D1 `AVAILABLE`; D2 `BUSY`; D3 `OFFLINE` | Only the `AVAILABLE` Driver is selected. | High |
| TC-DRQ-004 | Send driver requests | Do not send to a Driver with the wrong vehicle type | Ride requires `CAR`; a nearby Driver has a `BIKE` | 1. Send a driver search request.<br>2. Check which Driver receives the request. | Ride `vehicle_type=CAR`; Driver 201 `vehicle_type=BIKE` | Driver with the wrong vehicle type does not receive a request. | High |
| TC-DRQ-005 | Send driver requests | Ride is no longer in `SEARCHING_DRIVER` status | Ride = `DRIVER_ASSIGNED` | 1. Send `POST /rides/3001/driver-requests`.<br>2. Check the response. | `ride_id=3001`, status=`DRIVER_ASSIGNED` | HTTP `409 Conflict`.<br>Error response contains `code` and `message`.<br>No new request is sent. | High |
| TC-DRQ-006 | Send driver requests | Driver request is created with a 30-second timeout | Ride is searching for a driver | 1. Send a request.<br>2. Check `request_timeout_seconds` and `expires_at`. | Sent time `10:00:00`; timeout `30` | `request_timeout_seconds=30`.<br>`expires_at` is 30 seconds after the sent time. | High |
| TC-DRQ-007 | Driver response | Driver accepts a valid request | Request 7001 belongs to the current Driver; it has not expired; Ride has no Driver | 1. Driver logs in.<br>2. Send `POST /driver-requests/7001/respond`.<br>3. Check the response. | `{ "decision": "ACCEPTED" }` | HTTP `200 OK`.<br>`decision=ACCEPTED`.<br>Ride changes to `DRIVER_ASSIGNED`.<br>Driver changes to `BUSY`. | High |
| TC-DRQ-008 | Driver response | Driver declines a request | Request has not expired and belongs to the Driver | 1. Send a response.<br>2. Choose `DECLINED`.<br>3. Check the response. | `{ "decision": "DECLINED" }` | HTTP `200 OK`.<br>`decision=DECLINED`.<br>Ride remains `SEARCHING_DRIVER` if other drivers are available. | High |
| TC-DRQ-009 | Driver response | Driver responds after 30 seconds | Request is already `EXPIRED` | 1. Wait for the request to expire.<br>2. Send `ACCEPTED`.<br>3. Check the response. | `request_id=7001`; elapsed > 30s | HTTP `409 Conflict`.<br>Error indicates that the request has expired. | High |
| TC-DRQ-010 | Driver response | Another Driver attempts to respond to a request that is not theirs | Request 7001 belongs to Driver 201; Driver 202 is logged in | 1. Log in as Driver 202.<br>2. Send a response for request 7001. | `{ "decision": "ACCEPTED" }` | HTTP `403 Forbidden`.<br>Request and Ride are not changed. | High |
| TC-DRQ-011 | Acceptance conflict | Two Drivers accept the same Ride simultaneously | Ride is `SEARCHING_DRIVER`; requests were sent to D201 and D202 | 1. Send `ACCEPTED` for both requests simultaneously.<br>2. Check the results. | Request 7001 -> D201; Request 7002 -> D202 | Only the first valid request is `ACCEPTED`.<br>Ride has only one Driver.<br>The other request returns `409`. | High |
| TC-DRQ-012 | Driver response | Request was already accepted by another Driver | Ride = `DRIVER_ASSIGNED` | 1. Send `ACCEPTED` for the remaining request.<br>2. Check the response. | `request_id=7002` | HTTP `409 Conflict`.<br>The assigned Driver is not changed. | High |
