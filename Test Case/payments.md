## **TC05 - payments**

| **Test Case ID** | **Test Scenario** | **Test Case** | **Preconditions** | **Test Steps** | **Test Data** | **Expected Result** | **Priority** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-PAY-001 | Create Payment | Create a CASH payment for a completed Ride | Ride `COMPLETED`; final fare = 102000 | 1. POST payment.<br>2. Select CASH.<br>3. Check the response. | `ride_id=3001`; Header `Idempotency-Key=pay-001`; `{ "method":"CASH", "request_id":"pay-001" }` | HTTP `201`.<br>Payment has `method=CASH`, `status=CASH_PENDING`, and amount equal to the final fare. | High |
| TC-PAY-002 | Create Payment | Create an ONLINE payment | Ride `COMPLETED` | 1. POST payment.<br>2. Select ONLINE. | `{ "method":"ONLINE", "request_id":"pay-002" }` | HTTP `201`.<br>Status is `PENDING`.<br>Checkout information is returned if the system creates a checkout URL. | High |
| TC-PAY-003 | Create Payment | Create a payment when the Ride is not `COMPLETED` | Ride = `IN_PROGRESS` | 1. POST payment. | `ride_id=3001`, method `CASH` | HTTP `400`.<br>No valid payment is created. | High |
| TC-PAY-004 | Create Payment | Omit `method` | Ride `COMPLETED` | 1. POST payment.<br>2. Remove method. | `{ "request_id":"pay-003" }` | HTTP `400`. | High |
| TC-PAY-005 | Create Payment | Invalid method | Ride `COMPLETED` | 1. POST payment. | `method="BANK_TRANSFER"` | HTTP `400`. | High |
| TC-PAY-006 | Create Payment | Omit the Idempotency-Key | Ride `COMPLETED` | 1. POST payment without the header. | No `Idempotency-Key` | Request is rejected because the required header is missing. | High |
| TC-PAY-007 | Create Payment | Duplicate request ID | A payment with the request ID already exists | 1. Send a second request with the same request ID. | `request_id=pay-001` | HTTP `409 Conflict`.<br>A second payment is not created. | High |
| TC-PAY-008 | Cash confirmation | Driver confirms the exact amount | Payment `CASH_PENDING`; Driver is assigned | 1. POST cash-confirmation.<br>2. Enter the amount collected. | `collected_amount=102000` | HTTP `200`.<br>Status changes to `PAID`; `paid_at` is recorded. | High |
| TC-PAY-009 | Cash confirmation | Collected amount is lower than the final fare | Payment `CASH_PENDING` | 1. POST confirmation. | `collected_amount=100000`, expected `102000` | HTTP `400`.<br>Amount mismatch error.<br>Payment does not change to `PAID`. | High |
| TC-PAY-010 | Cash confirmation | Collected amount is higher than the final fare | Payment `CASH_PENDING` | 1. POST confirmation. | `collected_amount=105000`, expected `102000` | HTTP `400`.<br>Payment does not change to `PAID`. | High |
| TC-PAY-011 | Cash confirmation | Customer cannot confirm cash on behalf of the Driver | Payment `CASH_PENDING`; Customer is logged in | 1. Call the confirmation API. | `collected_amount=102000` | HTTP `403 Forbidden`. | High |
| TC-PAY-012 | Online callback | Valid SUCCESS callback | Payment `PENDING`; gateway signature is valid | 1. POST the callback.<br>2. Check the payment. | `result_code=SUCCESS`, amount=`102000`, valid signature | HTTP `200`.<br>Payment changes to `PAID`.<br>Provider transaction ID is recorded. | High |
| TC-PAY-013 | Online callback | FAILED callback | Payment `PENDING` | 1. POST the callback.<br>2. Set result = FAILED. | `result_code=FAILED`, amount=`102000` | HTTP `200`.<br>Payment changes to `FAILED`. | High |
| TC-PAY-014 | Online callback | Invalid signature | Payment `PENDING` | 1. POST the callback with an invalid signature. | `X-Payment-Signature=invalid-signature` | HTTP `400`.<br>Payment does not change to `PAID`. | High |
| TC-PAY-015 | Online callback | Missing signature header | Payment `PENDING` | 1. POST the callback.<br>2. Do not send `X-Payment-Signature`. | No header | Request is rejected. | High |
| TC-PAY-016 | Online callback | Callback amount differs from the payment amount | Payment amount = 102000 | 1. POST a SUCCESS callback.<br>2. Provide a different amount. | `amount=100000` | HTTP `409 Conflict`.<br>Payment does not change to `PAID`. | High |
| TC-PAY-017 | Online callback | Duplicate callback | SUCCESS callback was already processed | 1. Send the same callback for the transaction/payment again. | Same `provider_transaction_id` | HTTP `409 Conflict` or idempotent handling according to the API contract; no second payment state is created. | High |
