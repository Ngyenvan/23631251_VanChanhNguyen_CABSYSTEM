## **TC01 - authentication**

| **Test Case ID** | **Test Scenario** | **Test Case** | **Preconditions** | **Test Steps** | **Test Data** | **Expected Result** | **Priority** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TC-AUTH-001 | Account registration | Register a Customer account with all valid data | API is operational; phone number does not exist | 1. Send `POST /auth/register`.<br>2. Provide `full_name`, `phone`, `password`, and `role`.<br>3. Check the HTTP response and response body. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "password": "StrongPassword123", "role": "CUSTOMER" }` | HTTP `201 Created`.<br>Response returns a User with `user_id`, `full_name`, `phone`, `role`, and `status`.<br>Role = `CUSTOMER`.<br>Account is created successfully. | High |
| TC-AUTH-002 | Account registration | Register a Driver account with valid data | API is operational; phone number does not exist | 1. Send `POST /auth/register`.<br>2. Enter valid data with the Driver role.<br>3. Check the response. | `{ "full_name": "Tran Van B", "phone": "0912345678", "password": "DriverPass123", "role": "DRIVER" }` | HTTP `201 Created`.<br>Account is created successfully.<br>Response has `role = DRIVER`. | High |
| TC-AUTH-003 | Account registration | Omit `full_name` | API is operational | 1. Send a registration request.<br>2. Remove `full_name` from the JSON.<br>3. Check the response. | `{ "phone": "0901234567", "password": "StrongPassword123", "role": "CUSTOMER" }` | HTTP `400 Bad Request`.<br>Response has an Error structure containing `code` and `message`.<br>Account is not created. | High |
| TC-AUTH-004 | Account registration | Omit `phone` | API is operational | 1. Send a registration request.<br>2. Remove `phone`.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "password": "StrongPassword123", "role": "CUSTOMER" }` | HTTP `400 Bad Request`.<br>Validation error is returned.<br>Account is not created. | High |
| TC-AUTH-005 | Account registration | Omit `password` | API is operational | 1. Send a registration request.<br>2. Remove `password`.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "role": "CUSTOMER" }` | HTTP `400 Bad Request`.<br>Validation error is returned.<br>Account is not created. | High |
| TC-AUTH-006 | Account registration | Omit `role` | API is operational | 1. Send a registration request.<br>2. Remove `role`.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "password": "StrongPassword123" }` | HTTP `400 Bad Request`.<br>Validation error is returned.<br>Account is not created. | High |
| TC-AUTH-007 | Account registration | Password has exactly 8 characters | API is operational; phone does not exist | 1. Send a registration request.<br>2. Use a password with exactly 8 characters.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "password": "Abc12345", "role": "CUSTOMER" }` | HTTP `201 Created` if all other data is valid.<br>Password meets the minimum `minLength = 8`. | High |
| TC-AUTH-008 | Account registration | Password has fewer than 8 characters | API is operational | 1. Send a registration request.<br>2. Enter a 7-character password.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "password": "Abc1234", "role": "CUSTOMER" }` | HTTP `400 Bad Request`.<br>Validation error is returned because the password does not meet `minLength = 8`.<br>Account is not created. | High |
| TC-AUTH-009 | Account registration | Empty password | API is operational | 1. Send a registration request.<br>2. Set `password` to an empty string.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "password": "", "role": "CUSTOMER" }` | HTTP `400 Bad Request`.<br>Registration fails because the password is invalid. | High |
| TC-AUTH-010 | Account registration | Registration role is not in the allowed list | API is operational | 1. Send a registration request.<br>2. Provide a role outside the allowed enum.<br>3. Check the response. | `{ "full_name": "Nguyen Van An", "phone": "0901234567", "password": "StrongPassword123", "role": "ADMIN" }` | HTTP `400 Bad Request`.<br>Request is rejected because registration only allows `CUSTOMER` or `DRIVER`.<br>An Admin account cannot be created through registration. | High |
| TC-AUTH-011 | Account registration | `OPERATIONS` role cannot self-register | API is operational | 1. Send `POST /auth/register`.<br>2. Provide the `OPERATIONS` role.<br>3. Check the response. | `{ "full_name": "Operations User", "phone": "0923456789", "password": "StrongPassword123", "role": "OPERATIONS" }` | HTTP `400 Bad Request`.<br>Validation error is returned because `OPERATIONS` is not in the registration enum. | High |
| TC-AUTH-012 | Account registration | `ADMIN` role cannot self-register | API is operational | 1. Send `POST /auth/register`.<br>2. Provide the `ADMIN` role.<br>3. Check the response. | `{ "full_name": "Admin User", "phone": "0934567890", "password": "StrongPassword123", "role": "ADMIN" }` | HTTP `400 Bad Request`.<br>Request is rejected.<br>An Admin account cannot be created through registration. | High |
| TC-AUTH-013 | Account registration | Register with an existing phone number | A user with phone `0901234567` already exists | 1. Send `POST /auth/register`.<br>2. Use the existing phone number.<br>3. Check the response. | `{ "full_name": "User Duplicate", "phone": "0901234567", "password": "StrongPassword123", "role": "CUSTOMER" }` | HTTP `409 Conflict`.<br>Response indicates that the phone number is already registered.<br>A second user with the same phone is not created. | High |
| TC-AUTH-014 | Account registration | Verify phone number uniqueness | An account with phone `0901234567` already exists | 1. Send the first registration request with a valid phone.<br>2. Send a second request with the same phone but different information.<br>3. Compare the results. | First: `0901234567` + `User A`.<br>Second: `0901234567` + `User B`. | First request succeeds with `201` if the phone did not previously exist.<br>Second request returns `409 Conflict`.<br>Only one user exists with that phone. | High |
| TC-AUTH-015 | Account registration | Send a request without a request body | API is operational | 1. Send `POST /auth/register`.<br>2. Do not send a body.<br>3. Check the response. | Empty body | Request is rejected because `requestBody` is required.<br>HTTP `400 Bad Request` or the server's corresponding validation error. | High |
| TC-AUTH-016 | Account registration | Check the response after account creation | A new user can be created | 1. Send a valid registration request.<br>2. Check the entire response body.<br>3. Check the required User fields. | Valid registration payload | HTTP `201`.<br>Response contains `user_id`, `full_name`, `phone`, `role`, and `status`.<br>Password is not returned in the User response according to the API schema. | High |
| TC-AUTH-017 | Account registration | Check the status of a new account | Phone number does not exist | 1. Register a valid account.<br>2. Check the `status` field in the response. | Valid Customer registration | HTTP `201`.<br>User is created with a `status` value from the schema enum: `ACTIVE`, `INACTIVE`, or `LOCKED`. | Medium |
| TC-AUTH-018 | Login | Login with valid credentials | A valid account exists and can log in | 1. Send `POST /auth/login`.<br>2. Enter the correct phone and password.<br>3. Check the response body. | `{ "phone": "0901234567", "password": "StrongPassword123" }` | HTTP `200 OK`.<br>Response contains `access_token`, `token_type`, `expires_in`, and `user`.<br>Token is returned for APIs that require authentication. | High |
| TC-AUTH-019 | Login | Login with an incorrect password | Account `0901234567` exists | 1. Send `POST /auth/login`.<br>2. Enter the correct phone but an incorrect password.<br>3. Check the response. | `{ "phone": "0901234567", "password": "WrongPassword123" }` | HTTP `401 Unauthorized`.<br>Error response contains an appropriate `code` and `message` for invalid credentials.<br>No valid access token is issued. | High |
| TC-AUTH-020 | Login | Login with a non-existent phone number | API is operational; phone has not been registered | 1. Send `POST /auth/login`.<br>2. Enter a non-existent phone number.<br>3. Check the response. | `{ "phone": "0999999999", "password": "StrongPassword123" }` | HTTP `401 Unauthorized`.<br>Invalid credential error is returned.<br>No access token is issued. | High |
| TC-AUTH-021 | Login | Omit `phone` | API is operational | 1. Send a login request.<br>2. Remove `phone`.<br>3. Check the response. | `{ "password": "StrongPassword123" }` | Request is rejected because `phone` is required.<br>Login does not succeed. | High |
| TC-AUTH-022 | Login | Omit `password` | API is operational | 1. Send a login request.<br>2. Remove `password`.<br>3. Check the response. | `{ "phone": "0901234567" }` | Request is rejected because `password` is required.<br>Login does not succeed. | High |
| TC-AUTH-023 | Login | Send an empty request body | API is operational | 1. Send `POST /auth/login`.<br>2. Do not provide a request body.<br>3. Check the response. | Empty body | Request is rejected because the request body is required.<br>No token is issued. | High |
| TC-AUTH-024 | Login | Check the Login Response structure | Valid account can log in | 1. Log in with valid credentials.<br>2. Check the JSON response.<br>3. Check the required fields. | Valid phone/password | HTTP `200 OK`.<br>Response contains `access_token`, `token_type`, `expires_in`, and `user`.<br>`token_type` is `Bearer` according to the example/schema. | High |
| TC-AUTH-025 | Login | Check `expires_in` for the access token | Login succeeds | 1. Send a valid login request.<br>2. Read `expires_in` from the response.<br>3. Check its type and value. | Valid credentials | `expires_in` exists and has type `integer`.<br>Its value represents the access token expiration time. | Medium |
| TC-AUTH-026 | Login | Check User information in the Login Response | Login succeeds | 1. Log in with a valid account.<br>2. Check the `user` object.<br>3. Compare the required fields. | Valid credentials | The `user` object contains `user_id`, `full_name`, `phone`, `role`, and `status`.<br>Information matches the logged-in account. | High |
| TC-AUTH-027 | Login | Login with an account in `LOCKED` status | An account exists with `status = LOCKED` | 1. Send `POST /auth/login` using the locked account.<br>2. Enter the correct phone and password.<br>3. Check the response. | `{ "phone": "0945678901", "password": "StrongPassword123" }`<br>User status = `LOCKED` | According to the SRS, a locked account must not continue using system functions.<br>API must reject login and must not issue a valid access token. **Note:** `authentication.yaml` does not define a separate response for `LOCKED`; confirm whether the implementation uses `401` or a separate error code. | High |
| TC-AUTH-028 | Login | Verify that the password is not returned in the Login Response | Account exists and login succeeds | 1. Send a valid login request.<br>2. Check the complete JSON response.<br>3. Search for the `password` field. | Valid credentials | Response does not contain the `password` field.<br>User passwords are not exposed in the API response. | High |
| TC-AUTH-029 | Registration security | Verify that the password is not returned directly in the User response after registration | API is operational | 1. Register a valid account.<br>2. Check the response body.<br>3. Check whether a password field exists. | `{ "full_name": "Nguyen Van C", "phone": "0956789012", "password": "StrongPassword123", "role": "CUSTOMER" }` | HTTP `201`.<br>User response contains only fields defined by the `User` schema.<br>Password is not returned to the client. | High |
| TC-AUTH-030 | Login | Correct password with different letter casing | Account exists with password `StrongPassword123` | 1. Send a login request.<br>2. Change the password letter casing.<br>3. Check the response. | `{ "phone": "0901234567", "password": "strongpassword123" }` | HTTP `401 Unauthorized` if password authentication is case-sensitive.<br>No token is issued. | Medium |
